public class NoxenBridge {
    public static void main(String[] args) {
        String input = args.length > 0 ? args[0] : "No input";
        System.out.println("NOXEN Java Core: " + input);
    }
}
